# The OCR models should only be required for development (or for building the app from source)
# In that case, there are download scripts in ./src/ocr to get them
*.rten

# The images for testing OCR should also not be included
*.png
*.jpg